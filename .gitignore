# ---------- Node + build ----------
node_modules/
dist/
build/
.vite/
.cache/
coverage/

# ---------- TypeScript / tooling ----------
*.tsbuildinfo
/.turbo
/.parcel-cache

# ---------- Env + logs ----------
.env
.env.local
.env.*
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
*.log

# ---------- Editor / OS ----------
.vscode/
.idea/
.DS_Store
Thumbs.db

# ---------- Misc ----------
*.tgz

# ---------- Expo / React Native (mobile/) ----------
mobile/.expo/
mobile/.expo-shared/
mobile/dist/
mobile/web-build/
mobile/*.apk
mobile/*.aab
mobile/*.ipa
mobile/android/
mobile/ios/
mobile/*.jks
mobile/*.p8
mobile/*.p12
mobile/*.key
mobile/*.mobileprovision
mobile/*.orig.*
mobile/metro.config.local.js

# Expo/EAS local credentials
mobile/credentials.json
mobile/.eas/

# ---------- Backend (server/) ----------
server/dist/
server/logs/
server/*.pem

# ---------- MongoDB local dumps (if any) ----------
*.mongodb.gz
dump/